# Java-Class-Cleaner

A simple Core Java utility to delete all compiled `.class` files from a folder and its subfolders with a single run.

This tool is useful when practicing DSA or Java programs where many `.class` files get generated and need quick cleanup.

---

## 🚀 Features

- Deletes all `.class` files from the current directory
- Recursively cleans subfolders
- Uses only Core Java (`java.nio`)
- No arguments required
- Easy to run in VS Code or terminal

---

## 🛠 Requirements

- Java JDK 8 or higher
- VS Code / Terminal (Windows)

---

## 📂 Project Structure
'''
Java-Class-Cleaner/
│
├── deleteClass.java
└── README.md
'''
